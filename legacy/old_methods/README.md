# Archived Method Notes

The shared trainer, objective registry, centralized model factory, and unified evaluation stack remain in `src/ardg/`.

No runtime method implementation was deleted in this refactor. This directory exists to keep room for future archival of methods that fall outside the final thesis path.
