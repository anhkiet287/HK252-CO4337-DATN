runtime:
  profile: h100
experiment:
  platform: server
  device: cuda
  precision: bf16
dataset:
  data_dir: data
  num_workers: 16
eval:
  batch_size: 256
logging:
  output_dir: outputs/thesis/h100
  wandb:
    enabled: true
    mode: online
